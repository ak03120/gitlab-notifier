stages:
  - notify

notify_slack:
  stage: notify
  image: alpine:3.21
  tags:
    - tso
  script:
    - apk add --no-cache curl jq
    - sh scripts/notify_slack.sh
  rules:
    - if: '$CI_PIPELINE_SOURCE == "trigger" && $TRIGGER_PAYLOAD'
